{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^17.0.18",
    "concurrently": "^7.0.0",
    "lerna": "^4.0.0",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "build": "yarn workspaces foreach run build",
    "start": "concurrently -n server,peer \"cd packages/server && yarn run start:prod\" \"cd packages/server && yarn run peerjs\"",
    "dev": "concurrently -n client,server,peer \"cd packages/client && yarn dev\" \"cd packages/server && yarn dev\" \"cd packages/server && yarn run peerjs\"",
    "dev-yarn": "yarn workspaces foreach -pi --topological-dev --include '{@openrails/server, @openrails/client}' run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.1.1"
}
